# super-barnacle
blaze c wrapper attempt

Quick build Reference
```Bash
# 1. Install toolchains (first time only)
./build.sh --deps

# 2. Build the Windows DLL + .h package (default)
./build.sh

# 3. Clean and build both Windows DLL and Linux .so
./build.sh --clean --target all
```
